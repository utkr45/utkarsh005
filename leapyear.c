#include<stdio.h>
int main(){
    int year;
    printf("Enter the year  : \n");
    scanf("%d",&year);
    if(year == ){
        printf("not a leap year");
    }
    else{
        printf("is a leap year");
    }
    return 0;
}